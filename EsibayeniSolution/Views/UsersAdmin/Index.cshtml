@*@{
        ViewBag.Title = "Index";
    }

    <h2>Index</h2>*@

@{
    ViewBag.Title = "Users";
    Layout = "~/Views/Shared/_AdminLteLayout.cshtml";
}


@model IEnumerable<EsibayeniSolution.Models.ApplicationUser>



<div class=" row">
    <div class="col-lg-6">

    </div>
    <div class="col-lg-3"></div>
    <div class="col-lg-3">

        <b>

            @using (Html.BeginForm("Index", "UsersAdmin", FormMethod.Get))
            {
                <b> search By:</b>
                @Html.RadioButton("SearchBy", "Email", true) <text>Email</text>

                @Html.TextBox("Search")<input type="submit" , value="Search" />


            }
        </b>
    </div>
    @*<br />*@
</div>
@*<p style="text-decoration:none" class="btn btn-warning btn-sm">*@
@*<p>
    <a class="btn btn-primary" href="/UsersAdmin/Create">ADD USER<span class="glyphicon glyphicon-plus"></span></a>

   
</p>*@
<br />
<br />
<table class="table">
    <tr>

        <th>
            @Html.DisplayNameFor(model => model.Email)
        </th>

        <th>
            @Html.DisplayNameFor(model => model.UserName)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>

            <td>
                @Html.DisplayFor(modelItem => item.Email)
            </td>


            <td>
                @Html.DisplayFor(modelItem => item.UserName)
            </td>
            <td>
                @*<div>
                    <a class="btn btn-warning" href="/UsersAdmin/Edit/@item.Id">Edit<span class="glyphicon glyphicon-edit"></span></a>|
                </div>
                <div>
                     <a class="btn btn-danger" href="/UsersAdmin/Delete/@item.Id">Delete<span class="glyphicon glyphicon-trash"></span></a>
                </div>*@
                <div style="text-decoration:none" class="btn btn-warning btn-sm">
                    @Html.ActionLink("Edit", "Edit", new { id = item.Id }) |
                </div>
                @*<div style="text-decoration:none" class="btn btn-success btn-sm">
                    @Html.ActionLink("Details", "Details", new { id = item.Id }) |
                </div>*@
                <div style="text-decoration:none" class="btn btn-danger btn-sm">
                    @Html.ActionLink("Delete", "Delete", new { id = item.Id })
                </div>
            </td>
        </tr>
    }

</table>
